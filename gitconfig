[branch]
	sort = -committerdate
[alias]
    ll = log --oneline
    up = pull --rebase --autostash
    f = fetch
    s = status
    who = shortlog -sn --no-merges
[init]
    defaultBranch = main

[status]
	showStash = true

[includeIf "gitdir:~/work/"]
    path = ~/.gitconfig-work
