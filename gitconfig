[branch]
	sort = -committerdate
[alias]
    ll = log --oneline
    up = pull --rebase --autostash
    f = fetch
    s = status
[init]
    defaultBranch = main

[includeIf "gitdir:~/work/"]
    path = ~/.gitconfig-work
